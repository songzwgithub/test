# Historical Code Policy

Historical workflow code is not part of the active release tree.

The formal implementation is in `src/hengshui_insar/`. If historical code must
be inspected on this workstation, use the external non-release copy at:

`/tmp/hengshui_recovery_external/`

Do not commit recovered legacy workflows, old pipelines, or recovery tarballs to
the release repository.
