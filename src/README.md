# Source Packages

`src/hengshui_insar/` is the maintained release package.

Do not add human-facing one-off scripts here. Put runnable wrappers in
`commands/`, and put recovered historical workflow code under
`recovered_workflows/`.
