# Plotting

Recovered historical plotting helpers live here. Current release product checks are in `src/hengshui_insar/products.py`, and current plotting helpers are in `src/hengshui_insar/plotting.py`.

Use this directory only when rebuilding or auditing historical publication figures.
