# Legacy V2 Unbounded Pipeline

This directory preserves the historical pre-bounded V2 implementation for provenance only.

It is not the formal L01028 paper rerun path. The unbounded `exp(Ske)` formulation was superseded after spatial extrapolation failures, especially in formal validation fold 4. Use `recovered_workflows/pipelines/run_bounded_inversion.py` and the accepted bounded L01028 manifests for formal reproduction.
