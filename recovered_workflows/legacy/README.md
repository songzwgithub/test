# Legacy

This directory contains older provenance code. It is not the preferred entrypoint for the current L01028 release.

The current maintained package is `src/hengshui_insar/`. Historical code is retained because it documents earlier model-development paths and can be useful when reconstructing decisions.
